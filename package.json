{
  "name": "json-server",
  "version": "1.0.0",
  "description": "\"serveur api rest\"",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server db-users.json --routes routes.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
